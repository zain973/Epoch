version: '3.8'

services:
  # FastAPI Backend Service
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql://epoch_admin:epoch_secure_pass@db:5432/epoch_db
      - ENV=production
    depends_on:
      db:
        condition: service_healthy

  # PostgreSQL Database Service for the Chronological Feed
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_USER=epoch_admin
      - POSTGRES_PASSWORD=epoch_secure_pass
      - POSTGRES_DB=epoch_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U epoch_admin -d epoch_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
